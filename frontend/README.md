# NextStep Talent Frontend

React + Vite + Tailwind frontend for the NextStep Talent platform.

## Run
```bash
npm install
npm run dev
```

## Env
Create `.env` with:
```bash
VITE_API_URL=http://localhost:5000/api
```
